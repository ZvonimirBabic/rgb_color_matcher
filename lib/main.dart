import 'package:flutter/material.dart';

void main() {
  runApp(const RGBColorMatcher());
}

class RGBColorMatcher extends StatelessWidget {
  const RGBColorMatcher({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp();
  }
}
